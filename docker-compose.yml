services:
  rewritten:
    image: rewritten
    ports:
      - "5001:5001"
    volumes:
      - ./videos:/app/static/videos
      - ./database:/app/rewritten/database
    env_file:
      - .env
  newt:
    image: fosrl/newt
    container_name: newt
    restart: unless-stopped
    env_file:
      - .env
